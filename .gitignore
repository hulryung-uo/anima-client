/target
**/*.rs.bk

# web frontend
node_modules/
dist/
web/scene.json
web/scene.json.tmp

# stray CDP/debug helper scripts (local only)
/capchk.js
/rod.js
/tap.js

.DS_Store

# anima-desktop (Tauri): ACL/capability JSON schemas regenerated by
# tauri-build on every build — standard Tauri project .gitignore entry.
**/gen/schemas

# macOS signing/notarization key material — never commit (see docs/SIGNING.md).
# Kept only in the encrypted bundle you hold; this repo is public.
*.p12
*.p8
*.certSigningRequest
**/anima-signing/
